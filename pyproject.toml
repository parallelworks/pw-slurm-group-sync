[project]
name = "pw-groups"
version = "0.1.0"
description = "Sync ACTIVATE platform groups to Slurm accounts and associations"
requires-python = ">=3.11"
dependencies = [
    "parallelworks-client>=7.82.0",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]
